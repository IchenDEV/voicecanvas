# OpenAI Realtime voice input.
OPENAI_API_KEY=
OPENAI_REALTIME_MODEL=gpt-realtime-1.5

# Optional external Patch compiler.
# Leave these empty to use the built-in local mock compiler.
PATCH_COMPILER_API_KEY=
PATCH_COMPILER_BASE_URL=
PATCH_COMPILER_MODEL=
PATCH_COMPILER_PROVIDER=
