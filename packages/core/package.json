{
  "name": "@voicecanvas/core",
  "version": "0.0.0",
  "description": "Canvas document model, patch engine, validator, and Mermaid export for VoiceCanvas.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vitest": "^4.0.15"
  }
}
