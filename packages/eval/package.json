{
  "name": "@voicecanvas/eval",
  "version": "0.0.0",
  "description": "Acceptance cases and metric helpers for VoiceCanvas.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "alpha:report": "tsx src/alpha-report-cli.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@voicecanvas/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vitest": "^4.0.15"
  }
}
